[project]
name = "library-management-system"
version = "0.1.0"
description = "A simple library management system built with Django."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.4",
]
